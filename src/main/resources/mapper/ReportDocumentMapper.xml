<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.aimilvusweb.repository.ReportDocumentMapper">

    <insert id="insert" parameterType="com.example.aimilvusweb.entity.ReportDocument" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO report_document(title, source, institution, publish_date, created_at)
        VALUES(#{title}, #{source}, #{institution}, #{publishDate}, #{createdAt})
    </insert>

</mapper>
