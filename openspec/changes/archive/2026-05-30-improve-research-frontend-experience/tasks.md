## 1. 前端结构与文案

- [x] 1.1 将主导航、页面主标题和模块标题调整为“研报入库 / 投研分析 / 研报库”等投研业务语义。
- [x] 1.2 梳理 `App.vue` 中页面状态、computed 和 helper，新增状态映射与文本映射函数，确保未知值有兜底展示。
- [x] 1.3 弱化默认首屏中的技术栈说明，将接口代理、模型或向量库信息保留在运行状态或辅助说明中。

## 2. 研报入库页

- [x] 2.1 将上传页模板重组为文件选择、基础信息、投研标签和入库任务状态四个区域。
- [x] 2.2 上传成功后展示任务状态区域，包含 jobId 和查询最新任务状态入口。
- [x] 2.3 将任务状态查询结果从单行文本改为 OCR、切片、向量三个阶段状态标签。
- [x] 2.4 保持现有上传接口字段、任务状态接口和错误处理逻辑不变。

## 3. 投研分析页

- [x] 3.1 将快捷问题模板按投研任务类型或更易扫描的布局展示。
- [x] 3.2 将分析结果区拆分为分析状态、分析结论、证据质量和证据列表。
- [x] 3.3 将 inputIntent、outputLevel、degradationReasons 和 evidenceQuality 转译为中文业务提示。
- [x] 3.4 为证据列表增加摘要优先展示和按需展开完整片段的交互。
- [x] 3.5 增加纯前端复制结论能力，并在浏览器剪贴板失败时展示可理解提示。
- [x] 3.6 保持现有推荐流式接口、SSE 消费逻辑和清空/中止请求语义不变。

## 4. 研报库与诊断降噪

- [x] 4.1 将研报观测页重命名并重组为研报库，默认展示查询条件、视图切换和研报列表。
- [x] 4.2 在表格和卡片视图中使用 OCR、切片、向量阶段状态标签展示研报状态。
- [x] 4.3 增加选中研报摘要区域，展示标题、来源/机构、发布日期、错误信息和诊断操作。
- [x] 4.4 将处理链路改为用户点击后加载并展示，默认展示阶段、状态、耗时和错误摘要。
- [x] 4.5 将切片对照改为用户点击后加载并展示，长文本采用摘要优先或限制高度展示。
- [x] 4.6 保持 observations、ingest-stage-events 和 chunk-observation 现有接口调用方式不变。

## 5. 样式与响应式

- [x] 5.1 调整 `style.css` 中页面布局、区块、状态标签、证据卡、诊断面板和按钮样式。
- [x] 5.2 确保桌面端信息层级清晰，主要操作、次要操作和诊断入口视觉权重区分明确。
- [x] 5.3 确保窄屏下表单、按钮、证据卡和研报列表不出现文本重叠或不可操作问题。
- [x] 5.4 避免引入新的外部 UI 依赖，并保持现有视觉风格的克制和一致。

## 6. 验证

- [x] 6.1 执行前端构建验证：`npm run build`。
- [x] 6.2 启动前端页面并人工验证研报入库、投研分析、研报库、处理链路和切片对照的主要交互。
- [x] 6.3 在后端服务不可用时验证错误提示可理解，页面不会整体崩溃。
- [x] 6.4 执行 `mvn -q test`。
- [x] 6.5 执行 `openspec validate --all --strict`。
