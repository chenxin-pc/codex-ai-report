## 1. 数据模型与索引

- [x] 1.1 新增 `ingest_job` 表及状态/重试/调度字段。
- [x] 1.2 新增 `report_ingest_stage_event` 表，包含阶段观测字段与标题检索字段。
- [x] 1.3 为 `next_run_at`、`status`、`report_id`、`title_search_key` 增加必要索引。

## 2. 异步编排与状态机

- [x] 2.1 Upload/导入入口改为“写任务表并返回 job_id”。
- [x] 2.2 实现 OCR 定时任务：拉取可执行任务、执行、写状态与事件。
- [x] 2.3 实现 Chunk 定时任务：依赖 OCR 成功后触发，写状态与事件。
- [x] 2.4 实现 Vector 定时任务：依赖 Chunk 成功后触发，写状态与事件。

## 3. 重试与失败治理

- [x] 3.1 实现按阶段独立重试计数与退避策略（1m/5m/15m）。
- [x] 3.2 定义可重试/不可重试错误映射，失败转 `FAILED_FINAL`。
- [x] 3.3 增加幂等保护，避免重复执行与重复落库。

## 4. 观测与页面入口

- [x] 4.1 提供按 `report_id` 查询完整阶段时间线接口。
- [x] 4.2 提供按标题检索任务/事件能力（基于 `report_title_snapshot` / `title_search_key`）。
- [x] 4.3 在页面增加“处理链路”入口，展示阶段状态、耗时、模型、错误摘要。

## 5. 验证与迁移预留

- [x] 5.1 覆盖状态机、重试退避、幂等、事件落库的测试。
- [x] 5.2 增加积压与失败观测指标，验证慢点定位闭环。
- [x] 5.3 记录 Kafka 平移约束（保持状态机/幂等键/事件结构不变）。
