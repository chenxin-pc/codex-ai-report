## 1. 入口收敛与模块骨架

- [x] 1.1 在 `scripts/report-ingest-analysis` 下建立模块化目录，定义输入采集、导入客户端、状态管理、质量查询、导出与共享工具边界。
- [x] 1.2 调整 `commands/pipeline.py` 仅保留参数解析与流程编排，迁移底层实现到新模块。
- [x] 1.3 删除 `commands/import_reports_and_export_chunks.py`，清理相关引用。

## 2. 东方财富拉取能力接入

- [x] 2.1 在输入采集模块新增 `eastmoney` 模式，支持按配置拉取最多 10 篇研报并保存本地文件。
- [x] 2.2 将东方财富拉取结果标准化为现有 `ReportInput` 结构，记录来源信息与采集状态。
- [x] 2.3 复用现有导入 API 客户端完成导入，确保 run state 可追踪成功、失败与跳过项。

## 3. 导入后导出分析链路保持稳定

- [x] 3.1 抽取并复用 MySQL 查询与 Excel 写入公共能力，确保 `export-quality` 输出结构不变。
- [x] 3.2 验证东方财富来源导入后可继续执行质量导出与搜索评估命令。
- [x] 3.3 更新 README，移除旧兼容命令，补充统一入口与东方财富模式用法。

## 4. 测试与校验

- [x] 4.1 补充脚本级测试：东方财富采集成功、采集失败、导入状态记录、质量导出关键 sheet 结构。
- [x] 4.2 回归现有本地目录与 URL 清单模式，确保重构后行为不回退。
- [x] 4.3 执行 `mvn -q test` 与 `openspec validate --all --strict` 并修复问题。
