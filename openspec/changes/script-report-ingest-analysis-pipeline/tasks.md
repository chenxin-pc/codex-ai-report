## 1. 脚本结构与配置

- [x] 1.1 确定脚本入口形式和目录结构，新增批量导入、质量导出、搜索评估脚本骨架。
- [x] 1.2 新增脚本配置样例，包含输入模式、本地目录、URL 清单、后端 API 地址、数据库连接、评估 query 和输出目录。
- [x] 1.3 使用环境变量占位符管理密钥和密码，确保配置样例不包含真实敏感信息。
- [x] 1.4 新增脚本运行状态模型，记录 runId、输入清单、成功项、失败项、跳过项和输出文件路径。

## 2. 研报收集与批量导入

- [x] 2.1 实现本地目录输入模式，扫描 PDF 文件并生成最多 10 篇研报的输入清单。
- [x] 2.2 实现 URL 清单输入模式，下载最多 10 篇研报并记录来源 URL 和下载状态。
- [x] 2.3 为输入文件计算指纹或幂等键，支持重复运行时跳过已成功导入文件。
- [x] 2.4 实现调用后端导入接口的客户端，提交文件、标题、来源、机构和发布日期。
- [x] 2.5 记录每篇研报的 reportId、导入状态、失败阶段和错误摘要。
- [x] 2.6 支持遇错继续和遇错停止两种运行策略。

## 3. 数据查询与质量导出

- [x] 3.1 确认数据库中是否已具备 OCR 页级结果、清洗结果、chunk 诊断和过滤原因；缺失时补充 schema、实体、Mapper XML 和查询服务。
- [x] 3.2 实现按 runId、reportId 列表或时间范围拉取报告、OCR、chunk 和诊断数据。
- [x] 3.3 实现导入质量 Excel 导出，包含 reports、ocr_pages、chunks、filtered_chunks、summary sheet。
- [x] 3.4 对 OCR 原文和 chunkText 支持长度限制或摘要展示，并保留 reportId、pageNumber、chunkUid 追溯字段。
- [x] 3.5 确保日志和 Excel 不输出 API Key、Token、数据库密码或完整认证头。

## 4. 搜索评估脚本

- [x] 4.1 实现从配置读取评估 query 集合，未配置时记录跳过原因。
- [x] 4.2 实现调用后端推荐接口或受控评估入口，执行 query 搜索和推荐生成。
- [x] 4.3 记录每个 query 的执行状态、错误摘要、topK 召回结果、score/distance、chunk metadata 和推荐输出。
- [x] 4.4 搜索接口调用失败时记录失败阶段，并按配置决定继续或停止。
- [x] 4.5 实现搜索评估 Excel 导出，包含 queries、top_results、recommendations、manual_review sheet。
- [x] 4.6 manual_review sheet 预留是否相关、相关等级、问题备注、建议处理方式等人工评价字段。

## 5. 可重试与运行体验

- [x] 5.1 支持根据 runId 查看脚本运行摘要和输出文件位置。
- [x] 5.2 支持从已有 runId 重新执行质量导出或搜索评估，不重复导入研报。
- [x] 5.3 支持强制重跑参数，允许用户显式重新导入已存在研报。
- [x] 5.4 输出清晰的命令行进度、成功数量、失败数量、跳过数量和后续处理提示。
- [x] 5.5 编写脚本使用说明，覆盖本地目录模式、URL 清单模式、质量导出和搜索评估。

## 6. 测试与验证

- [x] 6.1 为本地目录扫描、URL 清单解析、下载失败和未满 10 篇场景补充测试。
- [x] 6.2 为幂等跳过、强制重跑、遇错继续和遇错停止补充测试。
- [x] 6.3 为导入接口客户端补充 mock 测试，覆盖成功、失败和异常响应。
- [x] 6.4 为数据库质量数据查询补充测试，确保 SQL 字段显式且参数绑定。
- [x] 6.5 为导入质量 Excel 和搜索评估 Excel 补充结构验证测试。
- [x] 6.6 运行 `mvn -q test` 并修复失败。
- [x] 6.7 运行 `openspec validate --all --strict` 并修复规格问题。
