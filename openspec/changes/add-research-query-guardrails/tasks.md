## 1. 输入可分析性判定

- [x] 1.1 定义投研输入判定结果枚举，覆盖直接分析、主题分析、不可分析。
- [x] 1.2 建立本地规则词典目录，维护拒绝词、投研动作词、主题词、行业词、股票代码模式和禁用建议词。
- [x] 1.3 实现轻量规则判定，识别问候、感谢、无投研意图等不可分析输入。
- [x] 1.4 实现宽松投研信号识别，覆盖公司名、股票代码、行业词、财务指标、估值、风险、对比、分析等意图。
- [x] 1.5 实现 LLM 兜底分类，仅在规则低置信时输出 intent、confidence、reason、normalizedQuery。
- [x] 1.6 增加 LLM 低置信、解析失败或调用失败时的保守引导策略。
- [x] 1.7 在同步推荐入口接入输入判定，不可分析输入在召回前短路返回投研模式引导。
- [x] 1.8 在流式推荐入口接入输入判定，不可分析输入通过流事件返回引导并发送 done。

## 2. 证据质量与主体一致性

- [x] 2.1 定义输出等级和降级原因结构，覆盖 L0、L1、L2、L3。
- [x] 2.2 定义证据质量状态，覆盖证据存在性、query 相关性、主体一致性、数据一致性和引用完整性。
- [x] 2.3 从召回结果 metadata、标题和 chunkText 中抽取公司名、股票代码、交易所、行业等主体信号。
- [x] 2.4 实现主体一致性校验，识别多标的混杂、代码/公司名冲突、交易所冲突和引用跨主体。
- [x] 2.5 实现证据存在性和 query 相关性校验，识别无证据、空证据和低相关召回。
- [x] 2.6 实现数据一致性校验，识别股价、市值、PE/PB、财报期、报告日期、预测年份和行业分类错配。
- [x] 2.7 实现引用完整性校验，识别 citation 缺失、无法对应 chunk 或跨主体引用。
- [x] 2.8 在推荐上下文构建阶段生成证据质量状态，并在硬闸门失败时降级到 L1。
- [x] 2.9 对主题分析输入生成主题分析状态，支持输出相关公司候选，并将投资动作建议限制为禁用。

## 3. 输出模板与 Prompt

- [x] 3.1 调整同步推荐输出逻辑，按 L0-L3 返回对应响应模板。
- [x] 3.2 调整流式推荐输出逻辑，确保降级状态、证据事件和正文语义一致。
- [x] 3.3 新增或更新输入分类 prompt，要求 LLM 仅输出结构化分类 JSON。
- [x] 3.4 更新推荐 system/user prompt，明确降级原因、禁用推荐语句和引用约束。
- [x] 3.5 为 L0、L1、L2 输出添加固定禁用词检查或生成后保护，禁止买入、增持、目标价、明确推荐、仓位建议等表述。

## 4. 前端与评估脚本

- [x] 4.1 前端展示推荐结果时区分拒答、信息不足、主题分析、相关公司候选和完整分析状态。
- [x] 4.2 更新流式推荐展示逻辑，确保不可分析输入能正常结束加载状态。
- [x] 4.3 更新搜索评估导出，增加输入判定、输出等级、降级原因、证据存在性、query 相关性、主体一致性、数据一致性和引用完整性字段。
- [x] 4.4 增加基准评估样例，至少覆盖“你好”“分析港口行业”“分析000582”“北部湾港和医药证据混杂”。

## 5. 测试与校验

- [x] 5.1 为输入判定补充单元测试，覆盖不可分析、主题分析、直接分析和边界短句。
- [x] 5.2 为 LLM 兜底分类补充单元测试，覆盖结构化分类、低置信、解析失败和调用失败。
- [x] 5.3 为证据质量校验补充单元测试，覆盖无证据、低相关、同主体、多主体、代码冲突、交易所冲突、数据错配和引用缺失。
- [x] 5.4 为同步推荐接口补充集成或服务测试，确认不可分析输入和 LLM 低置信输入不调用召回。
- [x] 5.5 为流式推荐接口补充测试，确认不可分析输入和 LLM 低置信输入发送引导和 done。
- [x] 5.6 执行 `mvn -q test`。
- [x] 5.7 执行 `openspec validate --all --strict`。
