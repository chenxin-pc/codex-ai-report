## 1. 输入可分析性判定

- [ ] 1.1 定义投研输入判定结果枚举，覆盖直接分析、主题分析、不可分析。
- [ ] 1.2 实现轻量规则判定，识别问候、感谢、无投研意图等不可分析输入。
- [ ] 1.3 实现宽松投研信号识别，覆盖公司名、股票代码、行业词、财务指标、估值、风险、对比、分析等意图。
- [ ] 1.4 在同步推荐入口接入输入判定，不可分析输入在召回前短路返回投研模式引导。
- [ ] 1.5 在流式推荐入口接入输入判定，不可分析输入通过流事件返回引导并发送 done。

## 2. 证据质量与主体一致性

- [ ] 2.1 定义输出等级和降级原因结构，覆盖 L0、L1、L2、L3。
- [ ] 2.2 从召回结果 metadata、标题和 chunkText 中抽取公司名、股票代码、交易所、行业等主体信号。
- [ ] 2.3 实现主体一致性校验，识别多标的混杂、代码/公司名冲突、交易所冲突和引用跨主体。
- [ ] 2.4 在推荐上下文构建阶段生成证据质量状态，并在证据污染或无引用时降级到 L1。
- [ ] 2.5 对主题分析输入生成标的未锚定状态，并将输出等级限制为 L2 或更低。

## 3. 输出模板与 Prompt

- [ ] 3.1 调整同步推荐输出逻辑，按 L0-L3 返回对应响应模板。
- [ ] 3.2 调整流式推荐输出逻辑，确保降级状态、证据事件和正文语义一致。
- [ ] 3.3 更新推荐 system/user prompt，明确降级原因、禁用推荐语句和引用约束。
- [ ] 3.4 为 L0、L1、L2 输出添加固定禁用词检查或生成后保护，禁止买入、增持、目标价、明确推荐、仓位建议等表述。

## 4. 前端与评估脚本

- [ ] 4.1 前端展示推荐结果时区分拒答、信息不足、主题观察和完整分析状态。
- [ ] 4.2 更新流式推荐展示逻辑，确保不可分析输入能正常结束加载状态。
- [ ] 4.3 更新搜索评估导出，增加输入判定、输出等级、降级原因和主体一致性字段。
- [ ] 4.4 增加基准评估样例，至少覆盖“你好”“分析港口行业”“分析000582”“北部湾港和医药证据混杂”。

## 5. 测试与校验

- [ ] 5.1 为输入判定补充单元测试，覆盖不可分析、主题分析、直接分析和边界短句。
- [ ] 5.2 为主体一致性校验补充单元测试，覆盖同主体、多主体、代码冲突和交易所冲突。
- [ ] 5.3 为同步推荐接口补充集成或服务测试，确认不可分析输入不调用召回。
- [ ] 5.4 为流式推荐接口补充测试，确认不可分析输入发送引导和 done。
- [ ] 5.5 执行 `mvn -q test`。
- [ ] 5.6 执行 `openspec validate --all --strict`。
